import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CelebrationPro - Event Management System',
  description: 'Comprehensive event management system for Indian functions and celebrations',
  keywords: ['event management', 'wedding planning', 'celebration', 'India', 'Hyderabad'],
  authors: [{ name: 'CelebrationPro Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
